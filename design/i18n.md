# i18n — 介面多語言支援

> 「介面多語言支援（i18n）」的設計說明。2026-09 完成實作：EN + zh-TW（預設）、提示詞雙語、設定面板語言切換。

## 目標

1. 網頁介面支援繁體中文（預設）與英文。
2. 分析相片的系統提示詞也翻譯為繁體中文，並依使用者語言選擇。
3. 有 i18n 基底，未來可繼續加入其他語言。

## 架構

### 翻譯字典 `src/lib/i18n/translations.ts`

- `Language = "en" | "zh-TW"`、`Messages`（單一 interface 列出所有 key，新增 key 時兩語系強制同時補齊，編譯期檢查）。
- `translations: Record<Language, Messages>` — en 字典與現行英文字串**逐字相同**（不改變既有英文行為）。
- `normalizeLanguage(value)` — 非 `"zh-TW"` 一律視為 `"en"`（容忍 localStorage 舊值）。
- `getMessages(language)` / `interpolate(template, vars)` — 非 React 工具，供 providers（無 context 環境）使用。
- `SUPPORTED_LANGUAGES` — UI 顯示用（label 用該語言自身名稱：English / 繁體中文）。

插值語法：`{var}` 佔位，`interpolate(t("page.analyzedWith"), { provider, model, seconds })`。

### React 存取 `src/hooks/useI18n.ts`

```ts
const { t, lang, setLanguage } = useI18n();
t("webgpu.manageModels")
t("page.analyzedWith", { provider, model, seconds: "3.2" })
```

- 語言**直接讀自 `AppSettings.language`**（欄位本來就存在，只是以前沒人用）→ 不另建 provider，`setLanguage` 就是 `updateSettings({ language })`，自動持久化到 localStorage。
- 所有 client 元件透過 `useI18n()` 取字串。

### `<html lang>` 同步

`src/components/AppProviders.tsx` 內 `LanguageTagSync`：`useEffect` 寫 `document.documentElement.lang`。

## 預設語言與遷移

- **預設 `zh-TW`**（owner 要求：網頁介面要有繁體中文）。
- `src/lib/settings-manager.ts`：沿用既有的 migration key 模式，新增 `what-do-you-see:migrated-language-zhtw` —— 一次性把已儲存的 `"en"`（舊預設值，代表使用者從未選擇過）改為 `"zh-TW"`。之後使用者在介面選擇的語言不會被再動。

## 提示詞翻譯 `src/lib/providers/system-prompt.ts`

**⚠️ 2026-09-21 回退：model-facing prompt 固定英文。** owner 回報 LFM2.5 全系列（450M/3B）在中文 prompt 下進入重複迴圈（貪心解碼確定性 → 同輸入必同輸出；正式站跑英文 prompt 的舊 build 無此問題，同瀏覽器同模型 → 成因鎖定中文 model prompt）。弱模型對中文指令的遵循能力不足，退化成分詞迴圈。新政策：`getSystemPrompt` **永遠回傳英文版**（attempt-2 baseline 逐字不動）；`buildUserPrompt` 固定英文主體 + 英文 EXIF 標籤，僅在 UI 語言 zh-TW 時**附加一條英文輸出語言指令**（"Respond in Traditional Chinese (繁體中文)..."）——指令用模型最強的語言、輸出語言跟隨 UI，UI i18n 與中文輸出結果都保留。

**⚠️ 2026-09-21 追加：LFM2.5 (WebGPU) 不附加該指令行。** 部署後 owner 驗證：同 build 同瀏覽器同照片，EN UI（無該行）正常、zh-TW UI（有該行）仍迴圈 → 該英文指令行本身是 LFM 迴圈的觸發源（已證實的 A/B）。Node CPU 重現測試（LFM2.5-VL-450M，greedy，四種指令變體）全部不迴圈 → 迴圈是 **WebGPU fp16 特有**（GPU 舍入改變 greedy token 選擇），CPU 無法可靠區分哪種 prompt 安全，因此不在本地盲測 prompt 變體。最終處理：`webgpu.ts` 對 `LiquidAI/LFM2.5*` 模型傳 `promptLanguage = "en"`（模型輸入與 EN UI 逐字相同 = 已驗證安全），中文輸出僅保留給強後端（Qwen3.5-4B WebGPU、llama-server）。若強模型也回報迴圈，再依同法逐模型停用。

**2026-09-21 attempt 3（`c49b9f5`）——owner 定案：保留。** owner 要求「再試一次讓 LFM 回覆中文，只改提示詞」。改動：zh-TW 時在系統提示詞**最末方**追加一句英文輸出語言指令（英文基底逐字不動）＋ **移除** LFM2.5 的 user-prompt zh 行抑制。owner 實測：450M 與 3B 中文輸出皆可正常、**均無迴圈**（3B 中英各測一次：55s/82s）——與上面「英文指令行是迴圈觸發源」的 A/B 結論不一致，該行與本句同時存在也無事，當年迴圈的成因仍不完全明確（推測與 WebGPU fp16 + 特定 prompt 組合有關，勿再盲信單行觸發論）。品質：450M 中文理解低下、不具使用價值（定位：WebGPU 驗證用最小模型）；**3B 中文「及格」，明顯優於 450M** → 3B 是 WebGPU 中文場景的實用模型。

以下原雙語方案僅供參考：

- 原英文版 **逐字保留**（attempt-2 baseline，owner 已驗證），僅補一個 `language` 參數路由。
- 新增 `PRIVACY_ANALYSIS_SYSTEM_PROMPT_ZH`：完整中譯（JSON 格式說明、Guidelines、Go deeper、deep-layer 規則全部對應），關鍵約束：
  - `paras` / `table` 這些 **JSON 欄位名維持英文**（解析器依賴）。
  - 末尾明確要求「以繁體中文輸出 paras 與 table 的內容」。
  - table 的 category 名稱會跟著模型輸出語言（中文提示詞 → 中文 category），`DataTableView` 直接顯示，無需改動。
- API：`getSystemPrompt(language?)` / `buildUserPrompt(exif, language?)` — 預設 `"en"`，舊呼叫點不傳參則行為不變。
- 兩個 live provider（`webgpu.ts`、`llama-server.ts`）改為 `getSystemPrompt(request.language)` + `buildUserPrompt(request.exif, request.language)`；`request.language` 由 `usePhotoAnalysis`（讀 `settings.language`）與 API route（form 欄位 `language`）傳入，兩者**本來就已在傳**，這次才真正生效。
- 英文提示詞**不加** "Respond in English"（保持 baseline 逐字不變，避免動到已驗證的行為）。

## Provider 錯誤訊息

- `webgpu.ts` 的錯誤訊息（無 model / 無影像 / generate 失敗等）用 `getMessages(request.language)` + `interpolate` 依語言輸出。
- `model-cache.ts` 的錯誤保持英文（技術性、且該層無 request 上下文）；`WebGPUSettings` 自己顯示的 "Download cancelled." / "Download failed" 等已轉為 i18n key。
- `/api/analyze` route（self-hosted only）的錯誤保持英文。

## 元件改動清單

全部改用 `t(...)`：`app/page.tsx`、`app/layout.tsx`（metadata description）、`settings/SettingsPanel.tsx`（新增「介面語言」段）、`settings/WebGPUSettings.tsx`、`settings/ProviderConfigForm.tsx`、`settings/ProviderSelector.tsx`、`settings/ConnectionTest.tsx`、`PhotoUpload.tsx`、`ExamplePhotos.tsx`、`EXIFDisplay.tsx`、`ModelDownloadDialog.tsx`、`DownloadProgress.tsx`、`AnalysisResult.tsx`、`DataTableView.tsx`、`LoadingAnimation.tsx`、`MapView.tsx`、`hooks/usePhotoAnalysis.ts`、`hooks/useModelDownload.ts`。

不變：`leaflet-map.tsx`（地圖 attribution 維持原文）、`site.ts` 的 `name`（品牌名）與 `GITHUB_REPO_URL`、範例照片的 credit（版權署名原文）、技術值（URL、model id、EXIF 數值）。

## 設定面板

「介面語言」放在 Settings 面板最上方，`ToggleButtonGroup`（English / 繁體中文），隨其他設定一起 Save 生效。

（待實作：語言切換將從設定面板移到 header 右上角下拉選單，見下方 section。）

## 新增語言的步驟

1. `Language` union 加 id；`SUPPORTED_LANGUAGES` 加 entry。
2. `translations` 加新語系（`Messages` interface 會強制補齊所有 key）。
3. 若提示詞也要支援：`system-prompt.ts` 加一份翻譯 + `getSystemPrompt` 路由。
4. `normalizeLanguage` 更新。

## 語言切換 → header 下拉選單（待實作，2026-09）

> TODO P1：把語言切換從設定面板移到網頁右上角、GitHub icon 左側，按下去開下拉選單選語言。

### 現況（2026-09 已驗證的代碼事實）

- 語言切換目前在 `SettingsPanel.tsx`：「介面語言」`ToggleButtonGroup`，走 draft 模式，隨面板 Save 才生效。
- header **不是獨立元件**，直接寫在 `src/app/page.tsx`：右側 cluster 目前是 `[GitHub icon] [Settings icon]` 兩個 `IconButton`。
- `useI18n()` 的 `setLanguage` = `updateSettings({ language })`，**直接寫 localStorage**（不等 Save）→ header 切換可以做到「點完立即生效」。
- `<html lang>` 同步（`AppProviders.tsx` 的 `LanguageTagSync`）監聽的是 `settings.language`，不關心底層是哪裡改的 → 無需動。

### 施工細節

1. **新元件 `src/components/LanguageSwitcher.tsx`**（client component，沿用一檔一元件慣例）：
   - 觸發器：MUI `IconButton` + `TranslateIcon`；`md` 以上在 icon 旁顯示目前語言名（`lang` 自身名稱：`繁體中文` / `English`），`xs` 只留 icon（手機寬度不擠破）。
   - 點擊開 MUI `Menu`：`SUPPORTED_LANGUAGES` 每語一筆 `MenuItem`，label 用該語言**自身名稱**（不翻譯），目前語言的項目加 `CheckIcon`。
   - 選取 → `setLanguage(id)`（立即持久化到 localStorage）＋關 Menu，**不需要 Save 按鈕**，全站字串隨 context 重新 render 即時切換。
   - a11y：`aria-haspopup="menu"`、`aria-label` 用新增 i18n key；鍵盤（Esc 關閉、方向鍵導航）由 MUI Menu 內建。
2. **`src/app/page.tsx`**：header 右側 cluster 在 GitHub icon **左側**插入 `<LanguageSwitcher />` → 順序 `[語言] [GitHub] [Settings]`。
3. **`SettingsPanel.tsx`**：移除「介面語言」段（`ToggleButtonGroup` + draft 的 `setLanguage`）。`settings.language` key、預設 zh-TW、一次性遷移邏輯全部不動。
4. **`translations.ts`**：只加 1 組 key `page.languageAria`（en: "Switch language" / zh-TW: 「切換語言」）。語言名稱 label 不需新 key（用 `SUPPORTED_LANGUAGES` 自身名稱）。

### 不動的東西

- 預設語言 zh-TW、migration key（`what-do-you-see:migrated-language-zhtw`）。
- 提示詞路由（`getSystemPrompt(language)`）— 語言來源仍是 `settings.language`，header 改了它自然生效，下次分析即用新語言提示詞。

### 驗收門檻

- 點選後全站字串立即改變（不重整頁面、不需 Save）；`<html lang>` 同步更新。
- 重整頁面後語言選擇維持（localStorage）。
- mobile（xs）：header 三個控制項不換行、不溢出（實機/DevTools 375px 驗）。
- `tsc --noEmit` 與 production build（`build:export`）通過。
